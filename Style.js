import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  green: {
    color: '#00b900'
  },
  container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
  }
})